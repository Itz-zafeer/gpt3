import React from 'react'

const ShortHeading = ({classes,title}) => {
    return (
        <>
            <h4 className={`heading34 ${classes}`}>{title}</h4>

        </>
    )
}

export default ShortHeading
